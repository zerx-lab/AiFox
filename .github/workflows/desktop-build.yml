# Manual desktop build for the Energy(CEF) app.
#
# Trigger: `workflow_dispatch` only. Pick a version string in the GitHub UI
# (e.g. v0.2.0) and the workflow will, per native runner:
#   1. Install the Energy CLI + CEF framework (cached under ~/.energy).
#   2. Build the renderer bundle + the Go/CEF binary (`task build`).
#   3. Produce a distributable via `energy package` (reads config/energy_*.json).
#   4. Upload each platform's `release/` tree as a workflow artifact.
#   5. Attach all artifacts to a freshly-created draft GitHub Release.
#
# Notes:
#   - The whole project lives at the repository root; every build step runs there.
#   - The verify gate (`task verify`) is intentionally skipped here; this
#     workflow assumes the requester validated locally.
#   - Cross-compilation is not attempted; each OS builds on its native runner.
#     macOS is out of scope for now (no signing).

name: desktop-build

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release tag (e.g. v0.2.0). A draft release will be created with this tag.'
        required: true
        type: string
      prerelease:
        description: 'Mark the draft release as a prerelease.'
        required: false
        type: boolean
        default: false

permissions:
  contents: write # required to create the draft GitHub Release.

# Steps run at the repository root (the project is not nested under a subdir).

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            label: linux-x64
          - os: windows-2022
            label: win32-x64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      # Energy(CEF) links GTK3 + X11 via CGO; the ubuntu runner lacks these
      # dev headers, so `task build` (go run . openapi) fails at pkg-config x11.
      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libx11-dev libxcb1-dev \
            libxkbcommon-dev libnss3-dev libasound2-dev

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Task
        uses: arduino/setup-task@v2
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}

      # Cache the CEF framework downloaded by `energy install` (~hundreds of MB).
      - name: Cache CEF framework
        uses: actions/cache@v4
        with:
          path: ~/.energy
          key: energy-${{ runner.os }}-v2.5.6

      # The energy CLI lives in the `cmd` sub-module of energye/energy and has no
      # standalone tag, so `go install .../v2/cmd/energy@<ver>` cannot resolve it.
      # Build it from the v2.5.6 source tree (matching go.mod) into GOBIN instead.
      # This is the v2 CLI that reads config/energy_*.json (designer's CLI wants
      # an incompatible .egp project file and is the wrong tool here).
      - name: Install Energy CLI + CEF framework
        shell: bash
        run: |
          git clone --depth 1 --branch v2.5.6 https://github.com/energye/energy.git "$RUNNER_TEMP/energy-src"
          (cd "$RUNNER_TEMP/energy-src/cmd" && go install ./energy)
          # The default registry energy.yanghy.cn has expired (energye/energy#72);
          # the maintainer's GitHub Pages mirror serves the same data tree.
          energy env -w Registry:https://energye.github.io/
          energy install --all

      - name: Install renderer deps
        run: pnpm install --frozen-lockfile
        working-directory: web

      - name: Build
        run: task build

      - name: Package
        run: energy package

      - name: Upload distributable
        uses: actions/upload-artifact@v4
        with:
          name: ai-fox-${{ matrix.label }}
          path: build/**/*
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Collect release assets
        id: collect
        working-directory: .
        run: |
          mkdir -p release-assets
          find artifacts -type f \
            \( -iname '*.exe' -o -iname '*.dmg' -o -iname '*.deb' \
               -o -iname '*.rpm' -o -iname '*.zip' -o -iname '*.tar.gz' \) \
            -exec cp -v {} release-assets/ \;
          ls -la release-assets/

      - name: Create draft release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: ${{ inputs.version }}
          draft: true
          prerelease: ${{ inputs.prerelease }}
          files: release-assets/*
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
